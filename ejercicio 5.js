var altura = 189;
var alturametros = (altura / 100);
console.log (alturametros)
var peso = 90.50;
var alturaredondeado = (((altura.toFixed(1)))/100);
console.log (alturaredondeado);
var alturaarriba = (Math.floor (peso));
console.log (alturaarriba);

var max = (Number.MAX_VALUE) + 1 ;
var maxnuevo = max.toString ();
var final = (maxnuevo = (Number.MAX_VALUE))
console.log (maxnuevo = (Number.MAX_VALUE))